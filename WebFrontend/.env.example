# Environment variables for CloudUnify Pro WebFrontend (Vite)
# Copy this file to `.env` and set values for your environment.
#
# IMPORTANT:
# This frontend uses Vite. API base must be provided via VITE_* variables.
# Older CRA-style variables (REACT_APP_*) are not used by this app.

# Base URL of the backend API (no trailing slash)
# Example: https://api.cloudunify.pro/api/v1
VITE_API_BASE=https://api.cloudunify.pro/api/v1
