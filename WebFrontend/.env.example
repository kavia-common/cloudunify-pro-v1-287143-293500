# Vite environment variables for CloudUnify Pro Web Frontend

# Base URL of the Backend API (must include scheme and base path)
# Examples:
#   Local dev: http://localhost:8000
#   Production: https://api.cloudunify.pro/api/v1
VITE_API_BASE=http://localhost:8000

# (Optional) WebSocket base URL. If omitted, it is derived from VITE_API_BASE
# by switching protocol to ws(s) and preserving the base path.
# Examples:
#   Local dev: ws://localhost:8000
#   Production: wss://api.cloudunify.pro/api/v1
VITE_WS_URL=ws://localhost:8000
