# Preview-friendly defaults.
#
# Vite only exposes env vars that start with the configured prefix.
# This project supports both VITE_* (preferred) and REACT_APP_* (legacy/Cra-style)
# via vite.config.ts -> envPrefix.

# Preferred: set explicit API base (includes /api/v1).
# If omitted, the app will fall back to '/api/v1' (relative), which only works if
# a proxy routes /api/v1 to the backend.
VITE_API_BASE=

# Alternative: set backend origin only; the app will append /api/v1.
# Example preview:
# VITE_BACKEND_URL=https://vscode-internal-XXXXX.beta01.cloud.kavia.ai:3001
VITE_BACKEND_URL=

# Optional WebSocket base; if empty, derived from API base where possible.
VITE_WS_URL=
