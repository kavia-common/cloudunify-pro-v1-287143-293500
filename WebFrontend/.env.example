# Frontend environment variables for Vite
# Rename this file to .env and edit as needed.

# Required: Backend base URL (including base path if applicable)
# Examples:
#   http://localhost:3001/api/v1
#   https://api.cloudunify.pro/api/v1
VITE_API_BASE=http://localhost:3001/api/v1

# Optional: WebSocket base. If not set, it is derived from VITE_API_BASE
# by switching protocol to ws(s) and preserving the base path.
# Examples:
#   ws://localhost:3001/api/v1
#   wss://api.cloudunify.pro/api/v1
VITE_WS_URL=ws://localhost:3001/api/v1
